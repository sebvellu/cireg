% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adfstat.R
\name{adfstat}
\alias{adfstat}
\title{Augmented Dickey Fuller test statistic (base implementation)}
\usage{
adfstat(tsrs, dvls = NULL, lags = 0)
}
\arguments{
\item{tsrs}{numeric vector. Time series to be tested for a unit root.}

\item{dvls}{optional numeric matrix. Deterministic regressors such as
a constant or trend. Must have the same number of rows as \code{tsrs}.}

\item{lags}{non negative integer. Number of lagged differences to include.}
}
\value{
A list with components:
\itemize{
\item \code{stat}: numeric scalar. ADF t statistic for the lagged level term.
\item \code{rsds}: numeric vector. Regression residuals from the full ADF model.
}
}
\description{
Computes the Augmented Dickey Fuller t statistic for the coefficient
on the lagged level term using the Frisch Waugh Lovell theorem.
Lagged differences and deterministic regressors are partialled out
before estimating the coefficient on the lagged level.
}
\details{
The function also returns the regression residuals from the full
ADF regression, which can be reused for information criteria based
lag selection.

The estimated regression is:
delta y_t = a * y_{t-1}
+ sum_{i=1}^lags b_i * delta y_{t-i}
+ deterministic terms
+ error_t

The statistic is computed after partialling out lagged differences
and deterministic regressors using QR decomposition.
}
\examples{
set.seed(1)
tsrs <- cumsum(rnorm(200))
adfstat(tsrs, lags = 2)

}
\keyword{internal}
