% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adflags.R
\name{adflags}
\alias{adflags}
\title{Select ADF lag length by information criteria}
\usage{
adflags(tsrs, dvls = NULL, maxl = NULL, crit = "aic")
}
\arguments{
\item{tsrs}{numeric vector. Time series to be tested for a unit root.}

\item{dvls}{optional numeric matrix. Deterministic regressors such as
a constant or trend. Must have the same number of rows as \code{tsrs}.}

\item{maxl}{non negative integer. Maximum number of lagged differences
considered. If \code{NULL}, a rule of thumb is used.}

\item{crit}{character string. Information criterion to use. Either
\code{"aic"} or \code{"bic"}.}
}
\value{
An integer. Selected number of lags.
}
\description{
Selects the number of lagged differences in the Augmented Dickey Fuller
regression using AIC or BIC. The function reuses the residuals produced
by adfstat to avoid rebuilding the regression repeatedly.
}
\details{
The information criterion is computed as:
smpl * log(rss / smpl) + penalty

where rss is the sum of squared residuals from the ADF regression,
smpl is the effective sample size, and the penalty is:
2 * k for AIC
log(smpl) * k for BIC

The parameter count k equals:
1 for the lagged level term
plus the number of lagged differences
plus the number of deterministic regressors
}
\examples{
\dontrun{
set.seed(1)
tsrs <- cumsum(rnorm(200))
dvls <- cbind(rep(1, length(tsrs)))
adflags(tsrs, dvls, maxl = 12, crit = "bic")
}

}
\keyword{internal}
